import { Helmet } from "react-helmet-async";

const Home = () => {
    return (
        <div>
            <Helmet>
                <title>Phi Book | Home</title>
            </Helmet>
        </div>
    );
};

export default Home;